---
title: 如何看这张矩阵表
---

| 状态 | 含义 |
| --- | --- |
| ✅ | 已在硬件上验证。该芯片支持的每一种记录类型，Fixture App都能读能写，包括已锁定的NDEF页面。 |
| ⚠️ | 部分支持。能读，不能写，通常是因为芯片出厂时带有Fixture App无法设置的厂商密码。 |
| ❌ | 不支持。芯片在发现阶段能被识别，会话在任何写入之前就结束，因此标签原样未动。 |
| — | 不适用。这款芯片没有 Fixture App 可以使用的 NDEF 区域。 |

状态是按芯片版本记录的，所以要看行里写的版本，而不是
包装上的系列名称。
